# Условия 
Конструкция в JS, которая позволяет совершить разные действия в зависимости от результата.
Пример:
```
a = 2;
if (a == 1) {
	console.log('a = 1');
} else {
	console.log('a != 1);
}
```

---
[[JS]]